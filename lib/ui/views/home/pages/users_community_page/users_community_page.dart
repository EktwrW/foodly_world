import 'package:flutter/material.dart';

class UsersCommunityPage extends StatelessWidget {
  const UsersCommunityPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Users Community'),
      ),
    );
  }
}
